using UnityEngine;

/// <summary>
/// Unique identifiers for managed UI windows.
/// Add new values here and assign them in the inspector on each UIWindow prefab.
/// </summary>
public enum WindowID
{
    None,
    Main,
    Levels,
    Shop,
    Achievements,
    Settings
}
